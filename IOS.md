# iOS Module Quick Reference

Note: iOS entries are included for completeness. This index does not host any iOS source — links back to original repos.

Template for each module entry

## Module: <module-name>
Short description: One-line summary.

Repo: https://github.com/owner/repo  
Original author/maintainer: name (email/handle)  
Status: Active / WIP / Archived / Deprecated  
Tags: ios, swift, cocoapods, xcode, etc.  
Capabilities (searchable): e.g., push-notifications, in-app-purchases
Last updated: YYYY-MM-DD

Supported systems (iOS)
- Minimum iOS version supported (e.g., iOS 12+)
- Device architectures: arm64, armv7 (if supported)

Per-system prerequisites (macOS + iOS)
- Xcode (version required) — `xcodebuild -version`
- CocoaPods / Carthage / SwiftPM — `pod --version` or `swift package --version`

Install / run (one-liners)
- `pod install && open Example.xcworkspace`

Quick verification
- Run simulator and check sample app

Known limitations
- e.g., "Requires paid Apple Developer account for device testing"

Attribution
- Link to the original repo — thanks to maintainers for their work.
